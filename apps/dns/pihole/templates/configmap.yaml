apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pihole.fullname" . }}-custom-dns
  labels:
    {{- include "pihole.labels" . | nindent 4 }}
data:
  # Custom local DNS records for homelab services
  # Add your local DNS entries here
  05-custom.conf: |
    # Local DNS for homelab services
    # Format: address=/hostname/ip
    # These will be populated once services have stable IPs
