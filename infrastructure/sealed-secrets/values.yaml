sealed-secrets:
  # Resource limits
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi

  # Metrics for Prometheus
  metrics:
    serviceMonitor:
      enabled: true
      namespace: monitoring
