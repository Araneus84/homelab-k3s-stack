apiVersion: v1
kind: ConfigMap
metadata:
  # Fixed name (not release-prefixed) so it always matches Argo/Helm and docs.
  name: grafana-dashboard-homelab-stack
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  homelab-stack-overview.json: |
{{ .Files.Get "dashboards/homelab-stack-overview.json" | indent 4 }}
